1. Demo Release
    -- User authentication;
    -- CRUD models;
    -- 100+ free downloadable themes;
    -- Pages:
        -- Home page;
        -- Registration/Login;
        -- Catalogue(shop, categories, search);
        -- Single item;
        -- About;
