Releases:
1. Demo
    -- Admin add member
    -- Attandance table;
    -- Permissions/Auth


    Models:
    1. User():
        name
        surname
        phone_number
        email
        profile_picture
        gender
        date_of_birth


    2. UserProfile():
        User(One2One)
        weight
        height
        biceps
        triceps
        breasts
        guts
        type(trainer/member)


    3. Plan():
        name
        description
        price
        days

    4. Subscription():
        User(FK)
        plan
        status
        start_date
        end_date

--------------------------------

2. First Release
    -- Register gyms
    -- Register gym staff
    -- Different plans for gyms
    -- Authorization system
    -- App startup data generator
    -- Turnstile

    -- Members:
        -- List filter
        -- List edit button
        -- Payment status
        -- Export
        -- Profile picture
        -- Add Member button
        -- Select and bulk actions [freeze accounts, cancel membership, send text]
        -- Joined at field
        -- Edit
        -- Member attendance as a bar chart with dates and length of session
        -- Current subscription start date and end date used and remaining sessions
        -- Payment history
        -- Sessions list, add session, delete session, edit session
        -- Message system
        -- Member unique 5 digit id
    -- Payment/Bills:
        -- Bill Model
        -- 



Bot TZ:
    Bot tili: Ru
    Ulanish: API orqali

    1. My Profile:
        -- Profile details (first name,
                            last name,
                            phone number,
                            birth date,
                            weight,
                            height,
                            gender,
                            profile pricture)

        -- Edit data with inline buttons(first name,
                                        last name,
                                        phone number,
                                        birth date,
                                        weight,
                                        height,
                                        gender,
                                        profile pricture)

    2. Subscription:
        -- Status
        -- Start date / End date
        -- Attended sessions / remaining sessions
        -- Sessions history

    3. QR Code:
        -- Generate QR code button




Bugs:
    -- User profile update tab old values must show up